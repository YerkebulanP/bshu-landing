# Значения только для `next dev` (в прод-сборку НЕ попадают).
# Кнопка «Вход для участников» на локалке ведёт на Vite-дев-сервер приложения
# (bsu/frontend, порт 5173) → http://localhost:5173/login.
# Личное переопределение — в .env.local (он в .gitignore).
NEXT_PUBLIC_APP_URL=http://localhost:5173
